# for download URL and release notes
GH_TOKEN = YOUR_GITHUB_TOKEN
# for the buy page
LEMSQZY_CHECKOUT_ID = YOUR_LEMONSQUEEZY_CHECKOUT_ID
# for fetching user info from Lemon Squeezy (feedback endpoint)
LEMSQZY_API_KEY = YOUR_LEMSQZY_API_KEY
# for storing feedback in Neon Postgres
NEON_DATABASE_URL = YOUR_NEON_DATABASE_URL
# shared secret between the desktop app backend and this API
FEEDBACK_API_KEY = YOUR_FEEDBACK_API_KEY
# bcrypt hash of the admin dashboard password (generate with: node -e "require('bcrypt').hash('yourpassword', 12).then(console.log)")
ADMIN_PASSWORD_HASH = YOUR_BCRYPT_PASSWORD_HASH
# random secret used to sign the admin session JWT (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ADMIN_SESSION_SECRET = YOUR_RANDOM_SESSION_SECRET
